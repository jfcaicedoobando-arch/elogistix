name: E2E (Playwright)

# Smoke E2E para los 5 flujos críticos. Se ejecuta:
#  - manualmente (workflow_dispatch)
#  - semanalmente, lunes 09:00 UTC (03:00 CDMX)
# No bloquea PRs. Si faltan secrets, el job se marca como skipped.

on:
  workflow_dispatch:
  schedule:
    - cron: "0 9 * * 1"

permissions:
  contents: read

concurrency:
  group: e2e-${{ github.ref }}
  cancel-in-progress: true

jobs:
  playwright:
    name: Playwright smoke
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    if: ${{ github.repository_owner != '' }}
    env:
      E2E_BASE_URL: ${{ secrets.E2E_BASE_URL }}
      E2E_EMAIL: ${{ secrets.E2E_EMAIL }}
      E2E_PASSWORD: ${{ secrets.E2E_PASSWORD }}
      E2E_PORTAL_EMAIL: ${{ secrets.E2E_PORTAL_EMAIL }}
      E2E_PORTAL_PASSWORD: ${{ secrets.E2E_PORTAL_PASSWORD }}
    steps:
      - name: Guard — required secrets present
        id: guard
        run: |
          if [ -z "${E2E_BASE_URL}" ] || [ -z "${E2E_EMAIL}" ] || [ -z "${E2E_PASSWORD}" ]; then
            echo "missing=true" >> "$GITHUB_OUTPUT"
            echo "::warning::E2E secrets missing — skipping run."
          else
            echo "missing=false" >> "$GITHUB_OUTPUT"
          fi

      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        if: steps.guard.outputs.missing == 'false'

      - name: Setup Bun + install
        if: steps.guard.outputs.missing == 'false'
        uses: ./.github/actions/setup-bun

      - name: Cache Playwright browsers
        if: steps.guard.outputs.missing == 'false'
        id: pw-cache
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('bun.lockb', 'bun.lock', 'package.json') }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      - name: Install Playwright browser (chromium)
        if: ${{ steps.guard.outputs.missing == 'false' && steps.pw-cache.outputs.cache-hit != 'true' }}
        run: bunx playwright install chromium

      - name: Install Playwright system deps
        # Las deps del SO (libs de Chromium) no se cachean en ~/.cache/ms-playwright,
        # así que se reinstalan siempre, incluso con cache hit de los binarios.
        if: steps.guard.outputs.missing == 'false'
        run: bunx playwright install-deps chromium

      - name: Run Playwright specs
        if: steps.guard.outputs.missing == 'false'
        run: bunx playwright test

      - name: Upload HTML report
        if: ${{ steps.guard.outputs.missing == 'false' && always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 14

      - name: Upload traces & videos
        if: ${{ steps.guard.outputs.missing == 'false' && failure() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: playwright-traces
          path: test-results/
          retention-days: 14
