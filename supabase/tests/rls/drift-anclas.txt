# Migraciones exentas del radar de drift por estar escritas con "anclas de
# texto" (parchan la definición viva con `replace(pg_get_functiondef(...))`).
#
# Por qué existe esta lista: una migración anclada sólo aplica si el texto de la
# función en ese punto de la historia coincide byte a byte. En una base limpia
# la definición previa puede venir de otra migración con formato distinto, así
# que la ancla no encuentra su bloque y la migración aborta — aunque el estado
# final de producción sea correcto.
#
# Regla: sólo se agrega una migración aquí cuando existe una migración POSTERIOR
# de reaplicación (sin anclas, o con anclas tolerantes + asserts finales) que
# garantiza el estado final en base limpia. El archivo NO es un permiso para
# escribir migraciones ancladas nuevas: úsense `CREATE OR REPLACE` completos.
#
# Un nombre de archivo por línea (basename), sin rutas.

# Ola 1 · parte 2 (parche a las RPC de lote). Estado final garantizado por
# 20260821005333_170f9c71-77e8-4f8d-8669-bfdb924110c2.sql.
20260821002652_60e70c49-9d28-49cd-a629-5170126f188f.sql

# Ola 2 · O2.2 (parche por texto a validar_cierre_embarque). Estado final
# garantizado por 20260826003000_ola2_comisiones_espejo.sql.
20260821010645_da0cff63-83ed-4870-bb70-73237a0ba039.sql

# Reparación de drift (v13.747.x): instala trg_cotizaciones_guard_en_operacion,
# pero la función public.cotizaciones_guard_en_operacion() nace en una migración
# POSTERIOR (20260901001400), así que en base limpia aborta. Estado final
# garantizado por 20260901001400_qa_remediacion_selectiva.sql (reaplica
# assert_nc_fecha_valida, conceptos_factura_assert_borrador, eerr_resumen_anual
# y la función + trigger del guard) y 20260902001000_qa_r2_r04_portal_papelera.sql.
20260827010526_7dabf435-2f49-4759-969e-c451be0d8ec8.sql
