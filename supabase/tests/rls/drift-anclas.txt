# Lista de migraciones exentas del radar de drift.
#
# Ola 9 (auditoría 3-3 · H8): la lista quedó VACÍA a propósito. Las cuatro
# migraciones que antes vivían aquí se volvieron tolerantes (las anclas de
# texto que no coinciden emiten NOTICE y se omiten; los objetos que nacen en
# migraciones posteriores se instalan bajo `IF EXISTS`), así que una base limpia
# aplica las 1100+ migraciones sin excepciones.
#
# REGLA: no se agregan migraciones nuevas a este archivo. Toda migración que
# cambie una función debe usar un `CREATE OR REPLACE FUNCTION` completo — está
# prohibido parchear definiciones con `replace(pg_get_functiondef(...))`.
#
# Un nombre de archivo por línea (basename), sin rutas.
