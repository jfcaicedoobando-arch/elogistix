# Auditoría visual UI/UX — 2026-08-24 (v13.733.0)

Alcance: **sólo look & feel** (consistencia, layout, componentes, micro-interacciones,
bugs visuales, a11y visual). No se auditó lógica de negocio ni datos.

Método:
1. Capturas Playwright de **rutas públicas** en 375 / 768 / 1440 px
   (`/`, `/login`, `/legal/*`, `/ayuda`, `/recursos/*`, `/sin-acceso`).
   Resultado: **0 overflow horizontal** y **0 errores de consola** en los 3 anchos.
2. Auditoría estática del código (`src/features`, `src/components`) sobre tokens,
   componentes canónicos, estados y touch targets.

> Limitación: el preview estaba **sin sesión** (`signed_out`), por lo que las pantallas
> autenticadas del ERP (embarques, CxP, CxC, tesorería, CRM, admin) **no se capturaron**.
> Los hallazgos de esos módulos provienen de la revisión de código.

---

## P1 · Bugs visuales

| # | Ubicación | Sev | Cat | Issue | Esperado |
|---|---|---|---|---|---|
| B-1 | `landingCopy.ts:6`, `LandingFooter.tsx:64`, 3 guías de `marketing` | Major | Bug | La bandera 🇲🇽 se renderiza como **dos cuadros vacíos (tofu)** en navegadores sin fuente de emoji-bandera (Windows/Chrome, headless). Visible en el eyebrow del hero. | Quitar el emoji o sustituirlo por un ícono SVG/texto ("Hecho en México"). |
| B-2 | `LandingHero` (375 px) | Minor | Layout | El bullet `•` de "Construido por forwarders mexicanos" queda **centrado verticalmente** respecto a las 2 líneas del texto, no alineado a la primera línea. | Marcador alineado al baseline de la primera línea (`items-start` / `list-inside`). |
| B-3 | 19 shells de página completa (`Layout.tsx:38`, `AuthCard.tsx:25`, `PortalLayout.tsx:47`, `LegalShell.tsx:31`, `Landing.tsx:36`, `AdminLayout.tsx:16`, …) | Minor | Layout | Usan `min-h-screen` en lugar de `min-h-dvh`: en móvil la barra de direcciones provoca salto de altura. Ya hay 6 archivos con `h-dvh`, patrón no propagado. | `min-h-dvh` en todos los shells. |

## P2 · Inconsistencia de componentes

| # | Ubicación | Sev | Cat | Issue | Esperado |
|---|---|---|---|---|---|
C-1 | `CierreDialogs.tsx:41,90`, `DialogSustituirFactura.tsx:100`, `NavieraQuickCreate.tsx:69`, `CancelarAnticipoDialog.tsx:45` | Major | Component | 5 modales **con inputs reales** armados a mano (`DialogHeader`/`DialogFooter`) en vez de `FormDialogShell`: sin icon-tile, sin footer sticky, anchos distintos. `CancelarAnticipoDialog` además no fija `dialogSize`. | Migrar a `FormDialogShell` + `FormDialogSection` (regla del proyecto). |
| C-2 | `UsuariosInternosDialogs.tsx:69`, `AdjuntoRow.dialogs.tsx:22,51`, `PnlTcAlinearDialog.tsx:38` | Minor | Component | Diálogos **sin token de ancho** (`dialogSize`), heredan el default de Radix → anchos que no coinciden con el resto. | Fijar `dialogSize.sm/md/lg`. |
| C-3 | 110 archivos / 252 usos de `text-xs` | Minor | Consistency | Convive con los tokens `text-label` / `text-body` ya definidos en `index.css`; dos escalas tipográficas en paralelo. Peores: `carteraColumns.tsx` (8), `clienteColumns.tsx` (8), `LandingPortal.tsx` (8), `LandingHero.tsx` (7). | Migrar `text-xs` a `text-label` (o `text-body` según jerarquía). |
| C-4 | 37 `style={{…}}` en 12 módulos (`VirtualTimeline.tsx` 3, `PortalEmbarqueStepper.tsx` 2, `OperadorCard.tsx` 2, `ClienteExpandible.tsx` 2, …) | Minor | Consistency | Estilos en línea; los estáticos deben ser clases (política del proyecto). | Sólo dejar los que calculan valores en runtime (ej. `width: %`). |

Nota positiva: **0 hex/rgba arbitrarios** en componentes, **0 `text-[Npx]`**, **0 tablas HTML crudas**
(todo pasa por `DataTable`), `PageHeader` en 81 archivos sin encabezados "a mano" en el ERP.

## P3 · Estados faltantes

| # | Ubicación | Sev | Cat | Issue | Esperado |
|---|---|---|---|---|---|
| S-1 | `ProtectedRoute.tsx:52`, `AgenteProtectedRoute.tsx:14`, `PortalProtectedRoute.tsx:12` | Major | Interaction | Pantalla completa con `Loader2` spinner (patrón duplicado 3 veces) en vez de skeleton → flash y CLS al hidratar. | Un solo componente de carga de ruta con `Skeleton`. |
| S-2 | `TablaContactos.tsx:62`, `NuevoClienteFormPieces.tsx:46`, `ResetPassword.tsx:102` | Minor | Interaction | Spinner en tabla/formulario aunque ya existe `DataTableBodySkeleton`. | Usar el skeleton del propio `DataTable`. |
| S-3 | `CxcAging.tsx`, `Comisiones.tsx`, `TabPnl.tsx`, `TabNotas.tsx`, `BandejaShell.tsx`, `BandejaPorTimbrar.tsx`, `Tesoreria.tsx`, `Compras.tsx` | Minor | Consistency | Copy de vacío/error escrito a mano en vez de `EmptyStateInline` / `ErrorStateInline` → tono y CTA distintos entre módulos (`CxcAging` y `Comisiones` fallan en ambos). | Componentes compartidos con botón "Reintentar". |

## P4 · Accesibilidad visual

| # | Ubicación | Sev | Cat | Issue | Esperado |
|---|---|---|---|---|---|
| A-1 | 29 overrides en 18 archivos: `PagoRepCell.tsx:35,42,43,55,72` (5× `h-6 w-6`), `ContactActions.tsx:37,61`, `TabNavieras/TabPuertos/TabTiposContenedor` (`h-8 w-8`), `FacturaNotasCreditoTable.tsx:112,123,144` (`h-7 w-7`), … | Major | A11y | El preset `size="icon"` ya da 44 px en móvil (`h-11 w-11 md:h-10`), pero los overrides lo bajan a 24–32 px en filas densas. | `min-h-11 min-w-11` en móvil, o agrupar acciones en un `DropdownMenu` de fila. |
| A-2 | `TarifaQuickApprovalButtons.tsx` (×2), `TabProyeccion.tsx` (×2), `PagoRepCell.tsx` (×2), `sidebar.tsx`, `portalUsuariosColumns.tsx`, `ExplicarHallazgoButton.tsx`, `cxpPorCapturarColumns.tsx`, `ConciliacionPagoCell.tsx`, `ArchivosEntranteChips.tsx`, `FacturaPagosTabla.tsx` | Major | A11y | 13 botones **icon-only sin `aria-label`**: lector de pantalla anuncia "botón" sin nombre. | `aria-label` descriptivo. |
| A-3 | `/ayuda` (1440) | Minor | A11y | Descripciones de sección y "Para: Todos" en gris muy claro sobre blanco; el tab inactivo "Glosario" casi no se distingue del activo. | `text-muted-foreground` y estado activo con contraste/borde claro. |

---

## Pendiente de auditar (requiere sesión en el preview)
Embarques (lista + detalle con tabs), CxP/Compras, CxC/Cartera, Tesorería, Profit,
Cotizaciones/Proformas, CRM Kanban, Configuración y Admin: hover de filas, z-index de
dropdowns dentro de modales, paginación, date pickers, dark mode y focus rings.
