/**
 * @deprecated v13.300.28 — usa `../fixtures/testBase` que compone la captura
 * de errores con `sessionIsolation`. Este archivo se mantiene como re-export
 * para no romper imports existentes.
 */
export { test, expect } from "./testBase";
