// Re-export del hook use-toast (convención shadcn) bajo el barrel hooks/shared
// para uniformidad. El módulo canónico sigue siendo `@/hooks/use-toast`.
export { useToast, toast } from "@/hooks/use-toast";
