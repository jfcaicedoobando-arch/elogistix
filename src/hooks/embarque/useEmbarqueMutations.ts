/**
 * Backward-compatible re-export del barrel `mutations/`.
 * Los nuevos consumidores deben importar desde `@/hooks/embarque/mutations`
 * (o desde el barrel del dominio: `@/hooks/embarque`).
 */
export * from "./mutations";
