/**
 * Re-export del diálogo compartido para mantener compatibilidad con los
 * imports existentes del portal. La lógica vive en
 * `@/components/shared/dialogs/CambiarPasswordDialog`.
 */
export { CambiarPasswordDialog } from "@/components/shared/dialogs/CambiarPasswordDialog";
