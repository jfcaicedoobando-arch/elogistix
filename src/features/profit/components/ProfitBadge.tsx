/**
 * @deprecated Movido a `@/components/shared/ProfitBadge` en el Bloque 2.3
 * (arquitectura). Este archivo re-exporta para compatibilidad temporal;
 * imports nuevos deben apuntar directo a `components/shared`.
 */
export { ProfitBadge, RentabilidadGlobalBadge } from "@/components/shared/ProfitBadge";
