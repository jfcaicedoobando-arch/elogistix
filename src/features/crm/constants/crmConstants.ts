/**
 * Constantes compartidas del módulo CRM.
 *
 * Se mantienen en un archivo puro (sin React) para evitar problemas de
 * resolución de imports named/default en el bundle y para facilitar pruebas.
 */
export const SIN_CLIENTE = "sin_cliente";
