/**
 * Fachada estable de los filtros de Oportunidades.
 * La fuente de verdad vive en `domain/oportunidades/filtros.ts` (v13.358.0)
 * para respetar la jerarquía de capas (domain no importa components).
 */
export {
  FILTROS_DEFAULT,
  type OportunidadesFiltros,
} from "@/features/crm/domain/oportunidades/filtros";
