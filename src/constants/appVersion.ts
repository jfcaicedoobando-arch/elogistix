/** Versión de la aplicación. Mantener sincronizada con CHANGELOG.md (raíz). */
export const APP_VERSION = "13.616.0";

/** Alias corto usado por observabilidad (Sentry release, reportes de error). */
export const n = APP_VERSION;
