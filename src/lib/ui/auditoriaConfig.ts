/**
 * Re-export canónico de la configuración de reglas de Auditoría.
 * Nueva ubicación preferida (Auditoría arquitectónica 13.56.6 / paso 11).
 * Los imports legacy desde `@/components/shared/utils/auditoriaConfig` siguen
 * funcionando porque el archivo original se mantiene como fuente.
 */
export * from "@/components/shared/utils/auditoriaConfig";
